import { AnimateOnActiveDirective } from './animate-on-active.directive';

describe('AnimateOnActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateOnActiveDirective();
    expect(directive).toBeTruthy();
  });
});
